
import UIKit

extension UIButton{
    func createRectangleButton() {
        let button = UIButton()// changes made here
        button.setTitle("Continue", for: .normal)
        button.backgroundColor = .clear
        button.tintColor = .blue
        //button.layer.borderWidth = 
    }
}
